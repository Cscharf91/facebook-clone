<%= form_with(model: @comment, local: true) do |f| %>
  <%= f.label :message, "Comment:" %><br>
  <%= f.text_area :message %>
  <%= f.hidden_field :post, params[:post_id] %>
  <%= f.submit %>
<% end %>